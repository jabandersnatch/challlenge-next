import React from 'react';

const PalindromeLayout = ({
  children,
}: {
  children: React.ReactNode;
}) => {
  return (
    <div>
      {children}
    </div>
  );
};

export default PalindromeLayout;
